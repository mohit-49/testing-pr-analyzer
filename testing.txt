test the file hellow how are duu alkdsjfalsdfjlkakjdsf


ion = useMutation({
    mutationFn: () => repositoryApi.connect(repoFullName, provider),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['repositories'] })
      router.push('/repositories')
    },
  })

  return (
    <div className="mx-auto">
      <div className="rounded-xl border border-gray-200 bg-white p-6 shadow-sm">
        <div>
          <Link href="/repositories">
            <button className="mb-4 inline-flex items-center gap-2 rounded-lg border border-gray-300 bg-white px-1.5 py-1.5 text-sm font-medium text-gray-700 shadow-sm hover:border-indigo-600 hover:bg-indigo-50 hover:text-indigo-600">
              <ArrowLeft className="h-4 w-4" />
            </button>
          </Link>
        </div>

        {/* Provider selector */}
        {/* <div className="mb-5">
          <label className="mb-1.5 block text-sm font-medium text-gray-700">Select Provider to connect the Repository</label>
          <div className="flex gap-2">
            {(['github', 'bitbucket'] as Provider[]).map((p) => (
              <button
                key={p}
                type="button"
                onClick={() => setProvider(p)}
                className={clsx(
                  'flex-1 rounded-lg border px-4 py-2.5 text-sm font-medium capitalize transition',
                  provider === p
                    ? 'border-indigo-500 bg-indigo-50 text-indigo-700'
                    : 'border-gray-300 text-gray-600 hover:border-gray-400'
                )}
              >
                {p === 'github' ? 'GitHub' : 'Bitbucket'}
              </button>
            ))}
          </div>
        </div> */}
        <div className="relative mb-6">
          <button
            type="button"
            onClick={() => setProviderOpen(!providerOpen)}
            className="flex w-72 items-center justify-between rounded-xl border border-gray-300 bg-white px-4 py-3 text-sm font-medium shadow-sm transition hover:border-indigo-500"
          >
            {provider
              ? provider === 'github'
                ? 'GitHub'
                : 'Bitbucket'
              : 'Select The Provider'}

            <ChevronDown
              className={`h-4 w-4 transition ${providerOpen ? 'rotate-180' : ''
                }`}
            />
          </button>

          {providerOpen && (
            <div className="absolute z-20 mt-2 w-72 overflow-hidden rounded-xl border border-gray-200 bg-white shadow-lg">
              <button
                className="w-full flex gap-2 items-center px-4 py-3 text-left text-sm bg-black text-white"
                onClick={() => {
                  setProvider('github')
                  setProviderOpen(false)
                }}
              >
                <GithubIcon className="h-4 w-4 text-white" /> GitHub
              </button>

              <button
                className="w-full flex gap-2 items-center px-4 py-3 text-left text-sm bg-[#2684ff] text-white"
                onClick={() => {
                  setProvider('bitbucket')
                  setProviderOpen(false)
                }}
              >
                 <BitbucketIcon className="h-4 w-4 text-white" />Bitbucket
              </button>
            </div>
          )}
        </div>

        {provider && (
          <>
            <div className="mb-4">
              <label className="mb-1.5 block text-sm font-medium text-gray-700">
                Repository Full Name
              </label>
              <div className="flex items-center gap-2 rounded-lg border border-gray-300 px-3 py-2 focus-within:border-indigo-500 focus-within:ring-1 focus-within:ring-indigo-500">
                <GitBranch className="h-4 w-4 flex-shrink-0 text-gray-400" />
                <input
                  type="text"
                  placeholder={provider === 'github' ? 'owner/repository-name' : 'workspace/repo-slug'}
                  value={repoFullName}
                  onChange={(e) => setRepoFullName(e.target.value)}
                  className="w-full text-sm outline-none placeholder:text-gray-400"
                />
              </div>
              <p className="mt-1.5 text-xs text-gray-500">
                {provider === 'github' ? 'Example: facebook/react' : 'Example: my-workspace/my-repo'}
              </p>
            </div>
          </>
        )}
        {connectMutation.isError && (
          <p className="mb-4 rounded-lg bg-red-50 px-3 py-2 text-sm text-red-600">
            Failed to connect. Make sure you have access to this repository{provider === 'bitbucket' ? ' and have logged in with Bitbucket' : ''}.
          </p>
        )}

        <button
          onClick={() => connectMutation.mutate()}
          disabled={!repoFullName.trim() || connectMutation.isPending}
          className="w-auto rounded-lg bg-indigo-600 py-2.5 px-4 text-sm font-semibold text-white hover:bg-indigo-700 disabled:cursor-not-allowed disabled:opacity-50"
        >
          {connectMutation.isPending ? 'Connecting...' : 'Connect Repository'}
        </button>
      </div>
    </div>
  )
}
