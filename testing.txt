Step 6: Agar sab sahi dikhe lekin phir bhi 503 aaye

Panel me hi "Restart App" button dabao (ye Apache ke proxy connection ko refresh kar deta hai) — kabhi kabhi backend restart hone ke baad bhi Apache ka proxy stale connection hold kar leta hai.

Step 7: Fix ke baad turant verify karo
bash
curl -I https://api-pranalyze.24livehost.com/api/auth/me

Agar response HTTP/2 401 (JSON wala) aaye — matlab fix ho gaya. Agar abhi bhi HTTP/2 503 (Apache HTML) aaye — to Step 4 ka screenshot bhej de mujhe (panel ka Node.js app details wala page), main dekh ke exact bata dunga.

Pehle Step 1-4 kar ke bata konsa panel hai aur us app ki current status/port kya dikh raha hai.

c panel access nhi h apne pass bss backend walo ne y 
frontend m ecosystem.config.js file de ti 

module.exports = {
  apps: [
    {
      name: 'web-ui',
      script: 'node_modules/next/dist/bin/next',
      args: 'start',
      env: {
        NODE_ENV: 'production',
      },
      env_production: {
        PORT: 4006, // Ensure this port is not already in use
        HOST: '0.0.0.0',
      },
    },
  ],
}
Show more
Navigated PM2 constraints, devised diagnostic strategy sans cPanel access
Navigated PM2 constraints, devised diagnostic strategy sans cPanel access

Ok bhai samajh gaya — tumhare paas sirf SSH/pm2 access hai, cPanel nahi. Ye backend ka ecosystem.config.js bhi confirm kar raha hai ki backend process khud sahi configured hai (port set hai), problem process me nahi hai — ye Apache proxy/domain routing ka hi issue hai, jo tumhare access ke bahar hai.

Chalo jo tumhare access me hai wahi se jitna dig kar sakte hain karte hain, phir baaki hosting/backend team ko bhejenge.

Step 1: Check karo koi .htaccess file hai jo readable ho
bash
find /home/pranalyze/public_html -iname ".htaccess" 2>/dev/null

Jo bhi mile, unka content dikha:

bash
cat /home/pranalyze/public_html/.htaccess 2>/dev/null
find /home/pranalyze -iname ".htaccess" -path "*pranalyze*" 2>/dev/null -exec cat {} \;

Kabhi kabhi subdomain ka proxy .htaccess se hi hota hai (mod_proxy directives), aur wo file tumhare user ke home directory me readable ho sakti hai.

Step 2: Apna backend ka actual listening port double-confirm karo
bash
netstat -tlnp 2>/dev/null | grep 5000

ya

bash
ss -tlnp | grep 5000

Confirm karo backend 5000 pe hi listen kar raha hai (jo .env me PORT=5000 hai).

Step 3: Backend team ko exact info bhejo

Chunki tumhare paas cPanel access nahi hai, ye info backend/hosting team ko forward karo taaki wo apna panel check kar sakein:

Subject: api-pranalyze.24livehost.com returning 503 — proxy routing issue

Backend Node app locally localhost:5000 pe perfectly kaam kar raha hai (confirmed via curl -I http://localhost:5000/api/auth/me → 401 Unauthorized with valid JSON response, matlab app healthy hai)
Same request public domain se karne par (curl -I https://api-pranalyze.24livehost.com/api/auth/me) Apache 503 Service Unavailable return kar raha hai (HTML error page, backend se aaya hi nahi)
Ye batata hai api-pranalyze.24livehost.com ka reverse proxy/Node.js App config localhost:5000 ko sahi se forward nahi kar raha
Request: cPanel/Setup Node.js App me api-pranalyze.24livehost.com ki entry check karo — confirm karo status "Started" hai aur assigned port 5000 se match karta hai. Agar mismatch hai to port sync kar do ya app restart kar do.
Step 4: Jab tak wo fix karein, tum bas ye monitor karte raho
bash
watch -n 5 'curl -s -o /dev/null -w "%{http_code}\n" https://api-pranalyze.24livehost.com/api/auth/me'

Ye har 5 second me status code print karega — jaise hi 503 se 401 me change ho, matlab fix ho gaya, turant pata chal jayega bina baar baar manually curl chalaye.

Step 1 aur 2 ke commands chala ke result bhi bhej de — agar .htaccess me kuch mila to shayad khud hi fix kar sakein bina panel access ke.

dekh bhai y pm2.config.js backend ki h 

module.exports = {
  apps: [
       {
      name: 'pr_review_api',
      script: 'node dist/app.js',
      env: {
        NODE_ENV: 'production',
      },
      env_production: {
        PORT: 4005,
        HOST: '0.0.0.0',
      },
    },
  ],
};

-- OR Y ecosystem.config.js frontend ki h 

module.exports = {
  apps: [
    {
      name: 'web-ui',
      script: 'node_modules/next/dist/bin/next',
      args: 'start',
      env: {
        NODE_ENV: 'production',
      },
      env_production: {
        PORT: 4006, // Ensure this port is not already in use
        HOST: '0.0.0.0',
      },
    },
  ],
}

-- ab mer ko bta step by step kya krna h

Show more
PM2 configuration mismatch detected between declared and actual ports
PM2 configuration mismatch detected between declared and actual ports

Bhai yehi mil gaya asli culprit! 🎯

Dekh carefully:

Backend pm2.config.js me likha hai: