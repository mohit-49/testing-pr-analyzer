test the file hellow how are duu alkdsjfalsdfjlkakjdsf


ion = useMutation({
    mutationFn: () => repositoryApi.connect(repoFullName, provider),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['repositories'] })
      router.push('/repositories')
    },
  })

  return (
    <div className="mx-auto">
      <div className="rounded-xl border border-gray-200 bg-white p-6 shadow-sm">
        <div>
          <Link href="/repositories">
            <button className="mb-4 inline-flex items-center gap-2 rounded-lg border border-gray-300 bg-white px-1.5 py-1.5 text-sm font-medium text-gray-700 shadow-sm hover:border-indigo-600 hover:bg-indigo-50 hover:text-indigo-600">
              <ArrowLeft className="h-4 w-4" />
            </button>
          </Link>
        </div>

        {/* Provider selector */}
        {/* <div className="mb-5">
          <label className="mb-1.5 block text-sm font-medium text-gray-700">Select Provider to connect the Repository</label>
          <div className="flex gap-2">
            {(['github', 'bitbucket'] as Provider[]).map((p) => (
              <button
                key={p}
                type="button"
                onClick={() => setProvider(p)}
                className={clsx(
                  'flex-1 rounded-lg border px-4 py-2.5 text-sm font-medium capitalize transition',
                  provider === p
                    ? 'border-indigo-500 bg-indigo-50 text-indigo-700'
                    : 'border-gray-300 text-gray-600 hover:border-gray-400'
                )}
              >
                {p === 'github' ? 'GitHub' : 'Bitbucket'}
              </button>
            ))}
          </div>
        </div> */}
        <div className="relative mb-6">
          <button
            type="button"
            onClick={() => setProviderOpen(!providerOpen)}
            className="flex w-72 items-center justify-between rounded-xl border border-gray-300 bg-white px-4 py-3 text-sm font-medium shadow-sm transition hover:border-indigo-500"
          >
            {provider
              ? provider === 'github'
                ? 'GitHub'
                : 'Bitbucket'
              : 'Select The Provider'}

            <ChevronDown
              className={`h-4 w-4 transition ${providerOpen ? 'rotate-180' : ''
                }`}
            />
          </button>

          {providerOpen && (
            <div className="absolute z-20 mt-2 w-72 overflow-hidden rounded-xl border border-gray-200 bg-white shadow-lg">
              <button
                className="w-full flex gap-2 items-center px-4 py-3 text-left text-sm bg-black text-white"
                onClick={() => {
                  setProvider('github')
                  setProviderOpen(false)
                }}
              >
                <GithubIcon className="h-4 w-4 text-white" /> GitHub
              </button>

              <button
                className="w-full flex gap-2 items-center px-4 py-3 text-left text-sm bg-[#2684ff] text-white"
                onClick={() => {
                  setProvider('bitbucket')
                  setProviderOpen(false)
                }}
              >
                 <BitbucketIcon className="h-4 w-4 text-white" />Bitbucket
              </button>
            </div>
          )}
        </div>

        {provider && (
          <>
            <div className="mb-4">
              <label className="mb-1.5 block text-sm font-medium text-gray-700">
                Repository Full Name
              </label>
              <div className="flex items-center gap-2 rounded-lg border border-gray-300 px-3 py-2 focus-within:border-indigo-500 focus-within:ring-1 focus-within:ring-indigo-500">
                <GitBranch className="h-4 w-4 flex-shrink-0 text-gray-400" />
                <input
                  type="text"
                  placeholder={provider === 'github' ? 'owner/repository-name' : 'workspace/repo-slug'}
                  value={repoFullName}
                  onChange={(e) => setRepoFullName(e.target.value)}
                  className="w-full text-sm outline-none placeholder:text-gray-400"
                />
              </div>
              <p className="mt-1.5 text-xs text-gray-500">
                {provider === 'github' ? 'Example: facebook/react' : 'Example: my-workspace/my-repo'}
              </p>
            </div>
          </>
        )}
        {connectMutation.isError && (
          <p className="mb-4 rounded-lg bg-red-50 px-3 py-2 text-sm text-red-600">
            Failed to connect. Make sure you have access to this repository{provider === 'bitbucket' ? ' and have logged in with Bitbucket' : ''}.
          </p>
        )}

        <button
          onClick={() => connectMutation.mutate()}
          disabled={!repoFullName.trim() || connectMutation.isPending}
          className="w-auto rounded-lg bg-indigo-600 py-2.5 px-4 text-sm font-semibold text-white hover:bg-indigo-700 disabled:cursor-not-allowed disabled:opacity-50"
        >
          {connectMutation.isPending ? 'Connecting...' : 'Connect Repository'}
        </button>
      </div>
    </div>
  )
}



test the file hellow how are duu alkdsjfalsdfjlkakjdsf



'use client'

import { useState } from 'react'
import { useMutation, useQueryClient } from '@tanstack/react-query'
import { repositoryApi } from '@/services/api/repository.api'
import { useRouter } from 'next/navigation'
import { ArrowLeft, ChevronDown, GitBranch } from 'lucide-react'
import Link from 'next/link'
import { clsx } from 'clsx'

type Provider = 'github' | 'bitbucket'

function BitbucketIcon({ className }: { className?: string }) {
  return (
    <svg viewBox="0 0 24 24" className={className} fill="#fff">
      <path d="M0.778 1.213a0.768 0.768 0 0 0-0.768 0.892l3.263 19.81c0.084 0.5 0.515 0.868 1.024 0.868h14.406c0.377 0 0.7-0.27 0.766-0.64l3.24-20.03a0.768 0.768 0 0 0-0.768-0.9H0.778zm14.424 14.4H8.798l-1.68-8.827h9.35l-1.266 8.827z"/>
    </svg>
  )
}
function GithubIcon({ className }: { className?: string }) {
  return (
    <svg viewBox="0 0 24 24" className={className} fill="currentColor" xmlns="http://www.w3.org/2000/svg">
      <path d="M12 0.297C5.373 0.297 0 5.67 0 12.297c0 5.302 3.438 9.8 8.205 11.387.6.113.82-.258.82-.577 0-.285-.01-1.04-.016-2.04-3.338.726-4.042-1.61-4.042-1.61-.546-1.387-1.333-1.757-1.333-1.757-1.09-.745.083-.729.083-.729 1.205.084 1.84 1.237 1.84 1.237 1.07 1.835 2.807 1.305 3.492.998.108-.775.418-1.305.76-1.605-2.665-.303-5.467-1.332-5.467-5.93 0-1.31.468-2.382 1.236-3.22-.124-.303-.536-1.524.117-3.176 0 0 1.008-.322 3.3 1.23a11.49 11.49 0 0 1 6.004 0c2.291-1.552 3.298-1.23 3.298-1.23.655 1.652.243 2.873.12 3.176.77.838 1.235 1.91 1.235 3.22 0 4.61-2.807 5.624-5.48 5.92.43.37.814 1.103.814 2.222 0 1.606-.015 2.902-.015 3.296 0 .322.216.694.825.576C20.565 22.092 24 17.596 24 12.297 24 5.67 18.627.297 12 .297z" />
    </svg>
  )
}

export default function ConnectRepositoryPage() {
  const [repoFullName, setRepoFullName] = useState('')
  // const [provider, setProvider] = useState<Provider>('github')
  const [provider, setProvider] = useState<Provider | null>(null)
  const [providerOpen, setProviderOpen] = useState(false)

  const router = useRouter()
  const queryClient = useQueryClient()

  const connectMutation = useMutation({
    mutationFn: () => repositoryApi.connect(repoFullName, provider),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['repositories'] })
      router.push('/repositories')
    },
  })

  return (
    <div className="mx-auto">
      <div className="rounded-xl border border-gray-200 bg-white p-6 shadow-sm">
        <div>
          <Link href="/repositories">
            <button className="mb-4 inline-flex items-center gap-2 rounded-lg border border-gray-300 bg-white px-1.5 py-1.5 text-sm font-medium text-gray-700 shadow-sm hover:border-indigo-600 hover:bg-indigo-50 hover:text-indigo-600">
              <ArrowLeft className="h-4 w-4" />
            </button>
          </Link>
        </div>

        {/* Provider selector */}
        {/* <div className="mb-5">
          <label className="mb-1.5 block text-sm font-medium text-gray-700">Select Provider to connect the Repository</label>
          <div className="flex gap-2">
            {(['github', 'bitbucket'] as Provider[]).map((p) => (
              <button
                key={p}
                type="button"
                onClick={() => setProvider(p)}
                className={clsx(
                  'flex-1 rounded-lg border px-4 py-2.5 text-sm font-medium capitalize transition',
                  provider === p
                    ? 'border-indigo-500 bg-indigo-50 text-indigo-700'
                    : 'border-gray-300 text-gray-600 hover:border-gray-400'
                )}
              >
                {p === 'github' ? 'GitHub' : 'Bitbucket'}
              </button>
            ))}
          </div>
        </div> */}
        <div className="relative mb-6">
          <button
            type="button"
            onClick={() => setProviderOpen(!providerOpen)}
            className="flex w-72 items-center justify-between rounded-xl border border-gray-300 bg-white px-4 py-3 text-sm font-medium shadow-sm transition hover:border-indigo-500"
          >
            {provider
              ? provider === 'github'
                ? 'GitHub'
                : 'Bitbucket'
              : 'Select The Provider'}

            <ChevronDown
              className={`h-4 w-4 transition ${providerOpen ? 'rotate-180' : ''
                }`}
            />
          </button>

          {providerOpen && (
            <div className="absolute z-20 mt-2 w-72 overflow-hidden rounded-xl border border-gray-200 bg-white shadow-lg">
              <button
                className="w-full flex gap-2 items-center px-4 py-3 text-left text-sm bg-black text-white"
                onClick={() => {
                  setProvider('github')
                  setProviderOpen(false)
                }}
              >
                <GithubIcon className="h-4 w-4 text-white" /> GitHub
              </button>

              <button
                className="w-full flex gap-2 items-center px-4 py-3 text-left text-sm bg-[#2684ff] text-white"
                onClick={() => {
                  setProvider('bitbucket')
                  setProviderOpen(false)
                }}
              >
                 <BitbucketIcon className="h-4 w-4 text-white" />Bitbucket
              </button>
            </div>
          )}
        </div>

        {provider && (
          <>
            <div className="mb-4">
              <label className="mb-1.5 block text-sm font-medium text-gray-700">
                Repository Full Name
              </label>
              <div className="flex items-center gap-2 rounded-lg border border-gray-300 px-3 py-2 focus-within:border-indigo-500 focus-within:ring-1 focus-within:ring-indigo-500">
                <GitBranch className="h-4 w-4 flex-shrink-0 text-gray-400" />
                <input
                  type="text"
                  placeholder={provider === 'github' ? 'owner/repository-name' : 'workspace/repo-slug'}
                  value={repoFullName}
                  onChange={(e) => setRepoFullName(e.target.value)}
                  className="w-full text-sm outline-none placeholder:text-gray-400"
                />
              </div>
              <p className="mt-1.5 text-xs text-gray-500">
                {provider === 'github' ? 'Example: facebook/react' : 'Example: my-workspace/my-repo'}
              </p>
            </div>
          </>
        )}
        {connectMutation.isError && (
          <p className="mb-4 rounded-lg bg-red-50 px-3 py-2 text-sm text-red-600">
            Failed to connect. Make sure you have access to this repository{provider === 'bitbucket' ? ' and have logged in with Bitbucket' : ''}.
          </p>
        )}

        <button
          onClick={() => connectMutation.mutate()}
          disabled={!repoFullName.trim() || connectMutation.isPending}
          className="w-auto rounded-lg bg-indigo-600 py-2.5 px-4 text-sm font-semibold text-white hover:bg-indigo-700 disabled:cursor-not-allowed disabled:opacity-50"
        >
          {connectMutation.isPending ? 'Connecting...' : 'Connect Repository'}
        </button>
      </div>
    </div>
  )
}


test the file hellow how are duu alkdsjfalsdfjlkakjdsf



'use client'

import { useState } from 'react'
import { useMutation, useQueryClient } from '@tanstack/react-query'
import { repositoryApi } from '@/services/api/repository.api'
import { useRouter } from 'next/navigation'
import { ArrowLeft, ChevronDown, GitBranch } from 'lucide-react'
import Link from 'next/link'
import { clsx } from 'clsx'

type Provider = 'github' | 'bitbucket'

function BitbucketIcon({ className }: { className?: string }) {
  return (
    <svg viewBox="0 0 24 24" className={className} fill="#fff">
      <path d="M0.778 1.213a0.768 0.768 0 0 0-0.768 0.892l3.263 19.81c0.084 0.5 0.515 0.868 1.024 0.868h14.406c0.377 0 0.7-0.27 0.766-0.64l3.24-20.03a0.768 0.768 0 0 0-0.768-0.9H0.778zm14.424 14.4H8.798l-1.68-8.827h9.35l-1.266 8.827z"/>
    </svg>
  )
}
function GithubIcon({ className }: { className?: string }) {
  return (
    <svg viewBox="0 0 24 24" className={className} fill="currentColor" xmlns="http://www.w3.org/2000/svg">
      <path d="M12 0.297C5.373 0.297 0 5.67 0 12.297c0 5.302 3.438 9.8 8.205 11.387.6.113.82-.258.82-.577 0-.285-.01-1.04-.016-2.04-3.338.726-4.042-1.61-4.042-1.61-.546-1.387-1.333-1.757-1.333-1.757-1.09-.745.083-.729.083-.729 1.205.084 1.84 1.237 1.84 1.237 1.07 1.835 2.807 1.305 3.492.998.108-.775.418-1.305.76-1.605-2.665-.303-5.467-1.332-5.467-5.93 0-1.31.468-2.382 1.236-3.22-.124-.303-.536-1.524.117-3.176 0 0 1.008-.322 3.3 1.23a11.49 11.49 0 0 1 6.004 0c2.291-1.552 3.298-1.23 3.298-1.23.655 1.652.243 2.873.12 3.176.77.838 1.235 1.91 1.235 3.22 0 4.61-2.807 5.624-5.48 5.92.43.37.814 1.103.814 2.222 0 1.606-.015 2.902-.015 3.296 0 .322.216.694.825.576C20.565 22.092 24 17.596 24 12.297 24 5.67 18.627.297 12 .297z" />
    </svg>
  )
}

export default function ConnectRepositoryPage() {
  const [repoFullName, setRepoFullName] = useState('')
  // const [provider, setProvider] = useState<Provider>('github')
  const [provider, setProvider] = useState<Provider | null>(null)
  const [providerOpen, setProviderOpen] = useState(false)

  const router = useRouter()
  const queryClient = useQueryClient()

  const connectMutation = useMutation({
    mutationFn: () => repositoryApi.connect(repoFullName, provider),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['repositories'] })
      router.push('/repositories')
    },
  })

  return (
    <div className="mx-auto">
      <div className="rounded-xl border border-gray-200 bg-white p-6 shadow-sm">
        <div>
          <Link href="/repositories">
            <button className="mb-4 inline-flex items-center gap-2 rounded-lg border border-gray-300 bg-white px-1.5 py-1.5 text-sm font-medium text-gray-700 shadow-sm hover:border-indigo-600 hover:bg-indigo-50 hover:text-indigo-600">
              <ArrowLeft className="h-4 w-4" />
            </button>
          </Link>
        </div>

        {/* Provider selector */}
        {/* <div className="mb-5">
          <label className="mb-1.5 block text-sm font-medium text-gray-700">Select Provider to connect the Repository</label>
          <div className="flex gap-2">
            {(['github', 'bitbucket'] as Provider[]).map((p) => (
              <button
                key={p}
                type="button"
                onClick={() => setProvider(p)}
                className={clsx(
                  'flex-1 rounded-lg border px-4 py-2.5 text-sm font-medium capitalize transition',
                  provider === p
                    ? 'border-indigo-500 bg-indigo-50 text-indigo-700'
                    : 'border-gray-300 text-gray-600 hover:border-gray-400'
                )}
              >
                {p === 'github' ? 'GitHub' : 'Bitbucket'}
              </button>
            ))}
          </div>
        </div> */}
        <div className="relative mb-6">
          <button
            type="button"
            onClick={() => setProviderOpen(!providerOpen)}
            className="flex w-72 items-center justify-between rounded-xl border border-gray-300 bg-white px-4 py-3 text-sm font-medium shadow-sm transition hover:border-indigo-500"
          >
            {provider
              ? provider === 'github'
                ? 'GitHub'
                : 'Bitbucket'
              : 'Select The Provider'}

            <ChevronDown
              className={`h-4 w-4 transition ${providerOpen ? 'rotate-180' : ''
                }`}
            />
          </button>

          {providerOpen && (
            <div className="absolute z-20 mt-2 w-72 overflow-hidden rounded-xl border border-gray-200 bg-white shadow-lg">
              <button
                className="w-full flex gap-2 items-center px-4 py-3 text-left text-sm bg-black text-white"
                onClick={() => {
                  setProvider('github')
                  setProviderOpen(false)
                }}
              >
                <GithubIcon className="h-4 w-4 text-white" /> GitHub
              </button>

              <button
                className="w-full flex gap-2 items-center px-4 py-3 text-left text-sm bg-[#2684ff] text-white"
                onClick={() => {
                  setProvider('bitbucket')
                  setProviderOpen(false)
                }}
              >
                 <BitbucketIcon className="h-4 w-4 text-white" />Bitbucket
              </button>
            </div>
          )}
        </div>

        {provider && (
          <>
            <div className="mb-4">
              <label className="mb-1.5 block text-sm font-medium text-gray-700">
                Repository Full Name
              </label>
              <div className="flex items-center gap-2 rounded-lg border border-gray-300 px-3 py-2 focus-within:border-indigo-500 focus-within:ring-1 focus-within:ring-indigo-500">
                <GitBranch className="h-4 w-4 flex-shrink-0 text-gray-400" />
                <input
                  type="text"
                  placeholder={provider === 'github' ? 'owner/repository-name' : 'workspace/repo-slug'}
                  value={repoFullName}
                  onChange={(e) => setRepoFullName(e.target.value)}
                  className="w-full text-sm outline-none placeholder:text-gray-400"
                />
              </div>
              <p className="mt-1.5 text-xs text-gray-500">
                {provider === 'github' ? 'Example: facebook/react' : 'Example: my-workspace/my-repo'}
              </p>
            </div>
          </>
        )}
        {connectMutation.isError && (
          <p className="mb-4 rounded-lg bg-red-50 px-3 py-2 text-sm text-red-600">
            Failed to connect. Make sure you have access to this repository{provider === 'bitbucket' ? ' and have logged in with Bitbucket' : ''}.
          </p>
        )}

        <button
          onClick={() => connectMutation.mutate()}
          disabled={!repoFullName.trim() || connectMutation.isPending}
          className="w-auto rounded-lg bg-indigo-600 py-2.5 px-4 text-sm font-semibold text-white hover:bg-indigo-700 disabled:cursor-not-allowed disabled:opacity-50"
        >
          {connectMutation.isPending ? 'Connecting...' : 'Connect Repository'}
        </button>
      </div>
    </div>
  )
}
